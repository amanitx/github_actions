name: Actions Tester

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  Linter:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Greeter
        uses: amanitx/actions-demo@main
        with:
          who-to-greet: Everyone!
